# tutorial-api-art

## Installation
```shell
npm init -y
npm install express --save
npm i cors --save
npm install moment-timezone --save
npm i jsonwebtoken --save
```

## Run
```shell
node index.js
```